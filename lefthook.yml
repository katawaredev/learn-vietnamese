# https://lefthook.dev/configuration/
# npx lefthook run pre-commit
pre-commit:
  parallel: true
  commands:
    check:
      run: npm run check
      stage_fixed: true
    typecheck:
      run: npm run typecheck
